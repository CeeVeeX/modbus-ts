{
  "name": "@modbus-ts/client",
  "version": "0.1.0",
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --target es2022 --dts --clean",
    "dev": "tsup src/index.ts --format esm --target es2022 --dts --watch",
    "lint": "eslint src --ext .ts",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modbus-ts/core": "workspace:*",
    "@modbus-ts/protocol": "workspace:*",
    "@modbus-ts/scheduler": "workspace:*",
    "@modbus-ts/subscription": "workspace:*",
    "@modbus-ts/utils": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "^6.0.3",
    "vitest": "^4.0.8"
  }
}
